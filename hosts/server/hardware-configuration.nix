# ⚠️ ARQUIVO GERADO — substitua pelo resultado de um destes comandos:
#
#   nixos-generate-config --show-hardware-config   (após bootar a máquina)
#   nixos-anywhere --generate-hardware-config nixos-facter ...  (instalação remota)
#
# O conteúdo real é específico do seu servidor (drives, rede, firmware).
{ ... }:
{ }
